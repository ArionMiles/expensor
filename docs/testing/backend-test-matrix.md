# Backend Test Matrix

| Domain | Routes | Critical Behaviors | Recommended Layer | Priority | Notes |
|---|---|---|---|---|---|
| Health, daemon, and bootstrap status | `GET /api/health`, `GET /api/status`, `GET /api/version`, `POST /api/daemon/start`, `POST /api/daemon/rescan`, `GET /api/config/active-reader` | health availability, status shape, daemon state transitions, active-reader bootstrap state | Unit, Component, Contract | High | low schema complexity, high operational visibility |
| Provider discovery | `GET /api/providers`, `GET /api/providers/thunderbird/discover/profiles`, `GET /api/providers/thunderbird/discover/mailboxes`, `GET /api/providers/{name}/guide` | provider listing, mailbox discovery, setup bootstrap shape | Unit, Contract | Medium | important for setup and onboarding entry points |
| Provider auth and configuration | `POST /api/providers/{name}/credentials`, `GET /api/providers/{name}/credentials/status`, `POST /api/providers/{name}/auth/start`, `GET /api/auth/callback`, `POST /api/providers/{name}/auth/exchange`, `GET /api/providers/{name}/auth/status`, `DELETE /api/providers/{name}/auth/token`, `GET /api/providers/{name}/config`, `PUT /api/providers/{name}/config`, `GET /api/providers/{name}/status`, `DELETE /api/providers/{name}` | auth flow edges, readiness state, validation failures, config persistence | Unit, Contract, Deferred Playwright | Medium | operationally noisy; split happy-path docs from full auth complexity |
| Dashboard and stats | `GET /api/stats/dashboard`, `GET /api/stats/charts`, `GET /api/stats/labels/monthly`, `GET /api/stats/heatmap` | seeded aggregates, query validation, shape stability | Component, Contract | High | strong candidate for seeded correctness tests |
| Config and settings | `GET /api/config/banks`, `POST /api/config/sync`, `GET /api/config/sync/status`, `GET /api/config/preferences`, `PATCH /api/config/preferences`, `GET /api/config/providers/{name}/checkpoint`, `DELETE /api/config/providers/{name}/checkpoint` | persistence, validation errors, default handling | Unit, Component, Contract | High | directly drives frontend settings behavior |
| Labels, categories, and buckets | `GET /api/config/labels/export`, `GET /api/config/labels/mappings`, `GET /api/config/labels`, `POST /api/config/labels`, `PUT /api/config/labels/{name}`, `DELETE /api/config/labels/{name}`, `PUT/DELETE /api/config/labels/{name}/merchant-mappings/{pattern}`, `GET /api/config/categories/export`, `GET /api/config/categories`, `POST /api/config/categories`, `DELETE /api/config/categories/{name}`, `PUT/DELETE /api/config/categories/{name}/merchant-mappings/{pattern}`, `GET /api/config/buckets`, `POST /api/config/buckets`, `DELETE /api/config/buckets/{name}`, `PUT/DELETE /api/config/buckets/{name}/merchant-mappings/{pattern}` | CRUD correctness, conflict behavior, export shape | Unit, Component, Contract | High | important for Transactions and settings flows |
| Rules | `GET /api/rules`, `GET /api/rules/export`, `POST /api/rules/import`, `POST /api/rules`, `PUT /api/rules/{id}`, `DELETE /api/rules/{id}` | regex validation, protected/default rule behavior, mutation success | Unit, Component, Contract | High | likely early frontend + backend test target |
| Transactions | `GET /api/transactions`, `GET /api/transactions/facets`, `GET /api/transactions/{id}`, `PATCH /api/transactions/{id}`, `POST /api/transactions/{id}/labels`, `DELETE /api/transactions/{id}/labels/{label}` | filtering and search correctness, URL-driven query mapping, mutation effects | Unit, Component, Contract | Highest | central user value and highest regression risk |
| Muted merchants | `GET /api/muted-merchants`, `POST /api/muted-merchants`, `PATCH /api/muted-merchants/{id}`, `DELETE /api/muted-merchants/{id}` | merchant-level mute behavior and reason persistence | Unit, Component, Contract | Medium | complements transaction muting |
| Merchant categorization | `POST /api/merchants/categorize` | validation, category/bucket propagation behavior | Unit, Component, Contract | Medium | likely paired with seeded taxonomy cases |
